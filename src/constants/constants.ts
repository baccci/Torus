export const PI = Number(Math.PI.toFixed(3))
export const HALF_PI = Number((Math.PI / 2).toFixed(3))
export const QUARTER_PI = Number((Math.PI / 4).toFixed(3))
export const THREE_QUARTERS_PI = Number((3 * Math.PI / 4).toFixed(3))
export const CHANGE_RATE = 0.05
export const POINT_SHAPES = {
  CIRCLE: 'circle',
  SQUARE: 'square'
} as const